datasource db {
   provider = "mysql"
   url      = "mysql://root:root@localhost:3306/prisma-graphql"
}

generator photonjs {
   provider = "photonjs"
}

model User {
   id    String  @id @default(cuid())
   name  String?
   email String  @unique
}
